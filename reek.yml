---
  detectors:
    IrresponsibleModule:
      enabled: false

    FeatureEnvy:
      enabled: false

    DuplicateMethodCall:
      max_calls: 3

    Attribute:
      enabled: false

  exclude_paths:
    - db/
